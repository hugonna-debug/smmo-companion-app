--- src/pages/WatchlistPage.tsx
+++ src/pages/WatchlistPage.tsx
@@ -165,6 +165,11 @@
           <button
             key={key}
             type="button"
+            aria-label={`Sort by ${
+              key === "level" ? "Level" : key === "str" ? "STR" : key === "lastActivity" ? "Activity" : "Name"
+            } ${
+              sortBy === key ? (sortAsc ? "(ascending)" : "(descending)") : ""
+            }`}
             onClick={() => {
               if (sortBy === key) setSortAsc(!sortAsc);
               else {
